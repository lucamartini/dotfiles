mkdir -p nwg/full-backup/full-backup && git clone git@ci.nwgitalia.it:etl/full-backup.git nwg/full-backup/full-backup
mkdir -p ticket/ticket && git clone git@ci.nwgitalia.it:nwg/ticket.git ticket/ticket
mkdir -p etl/ivd-snapshots/ivd-snapshots && git clone git@ci.nwgitalia.it:etl/ivd-snapshots.git etl/ivd-snapshots/ivd-snapshots
mkdir -p crm-mysql/crm-mysql && git clone git@ci.nwgitalia.it:etl/crm-mysql.git crm-mysql/crm-mysql
mkdir -p aws/zip && git clone git@ci.nwgitalia.it:services/zip.git aws/zip
mkdir -p vipoffice/vipoffice && git clone git@ci.nwgitalia.it:nwg/vipoffice.git vipoffice/vipoffice
mkdir -p gmd/gmd && git clone git@ci.nwgitalia.it:apis/gmd.git gmd/gmd
mkdir -p nwg/nwg-cluster-mgmt/nwg-cluster-mgmt && git clone git@ci.nwgitalia.it:env/nwg-cluster-mgmt.git nwg/nwg-cluster-mgmt/nwg-cluster-mgmt
mkdir -p search/search && git clone git@ci.nwgitalia.it:apis/search.git search/search
mkdir -p nwg/nwg-private-config/nwg-private-config && git clone git@ci.nwgitalia.it:package/backend/nwg-private-config.git nwg/nwg-private-config/nwg-private-config
mkdir -p nwg/nwg-model/nwg-model && git clone git@ci.nwgitalia.it:package/common/nwg-model.git nwg/nwg-model/nwg-model
mkdir -p nwg/nwg-bpl/nwg-bpl && git clone git@ci.nwgitalia.it:package/backend/nwg-bpl.git nwg/nwg-bpl/nwg-bpl
mkdir -p services/users/users && git clone git@ci.nwgitalia.it:services/users.git services/users/users
mkdir -p services/proposals/proposals && git clone git@ci.nwgitalia.it:services/proposals.git services/proposals/proposals
mkdir -p services/news/news && git clone git@ci.nwgitalia.it:services/news.git services/news/news
mkdir -p services/myfocus/myfocus && git clone git@ci.nwgitalia.it:services/myfocus.git services/myfocus/myfocus
mkdir -p services/leads/leads && git clone git@ci.nwgitalia.it:services/leads.git services/leads/leads
mkdir -p services/ivd/ivd && git clone git@ci.nwgitalia.it:services/ivd.git services/ivd/ivd
mkdir -p services/istat/istat && git clone git@ci.nwgitalia.it:services/istat.git services/istat/istat
mkdir -p services/influencers/influencers && git clone git@ci.nwgitalia.it:services/influencers.git services/influencers/influencers
mkdir -p services/incentives/incentives && git clone git@ci.nwgitalia.it:services/incentives.git services/incentives/incentives
mkdir -p services/hirings/hirings && git clone git@ci.nwgitalia.it:services/hirings.git services/hirings/hirings
mkdir -p services/fv/fv && git clone git@ci.nwgitalia.it:services/fv.git services/fv/fv
mkdir -p services/esolver/esolver && git clone git@ci.nwgitalia.it:services/esolver.git services/esolver/esolver
mkdir -p services/energy/energy && git clone git@ci.nwgitalia.it:services/energy.git services/energy/energy
mkdir -p services/courses/courses && git clone git@ci.nwgitalia.it:services/courses.git services/courses/courses
mkdir -p services/consultings/consultings && git clone git@ci.nwgitalia.it:services/consultings.git services/consultings/consultings
mkdir -p services/app_eb/app_eb && git clone git@ci.nwgitalia.it:services/app_eb.git services/app_eb/app_eb
mkdir -p services/anter/anter && git clone git@ci.nwgitalia.it:services/anter.git services/anter/anter
mkdir -p services/accounts/accounts && git clone git@ci.nwgitalia.it:services/accounts.git services/accounts/accounts
mkdir -p services/charun/charun && git clone git@ci.nwgitalia.it:services/charun.git services/charun/charun
mkdir -p frontends/eb/eb && git clone git@ci.nwgitalia.it:nwg/eb.git frontends/eb/eb
mkdir -p frontends/accounts/accounts && git clone git@ci.nwgitalia.it:nwg/accounts.git frontends/accounts/accounts
mkdir -p app/influencer-green/influencer-green && git clone git@ci.nwgitalia.it:nwgenergia/influencer-green.git app/influencer-green/influencer-green
mkdir -p aws/fv-docs-to-deloitte/fv-docs-to-deloitte && git clone git@ci.nwgitalia.it:services/fv-docs-to-deloitte.git aws/fv-docs-to-deloitte/fv-docs-to-deloitte
